'use strict';

/**
 * scholarship-editor service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::scholarship-editor.scholarship-editor');
